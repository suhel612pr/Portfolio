avatar.jpg is in use — rendered in the hero via `profile.avatarUrl` in
src/data/profile.ts. Replace this file (keep the same filename, or update
the path in profile.ts) if you want to swap it for a different photo later.
