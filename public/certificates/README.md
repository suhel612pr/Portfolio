In use — see src/data/certificates.ts. Each entry's `image` is the
web-sized thumbnail shown on the card; `pdf` (when present) is the
original file used for the "View certificate" link. Add new certificates
here and add a matching entry to certificates.ts.
